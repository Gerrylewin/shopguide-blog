## 2024-07-29 - Added focus-visible to ImageGallery and fix Next Image

**Learning:** Added `focus-visible` to interactive elements to improve keyboard accessibility and replaced standard `img` with Next `Image` component to ensure optimal performance and fix a linting error.
**Action:** Always apply `focus-visible` to custom interactive elements for better accessibility and use `next/image` when possible.
